import { StyleSheet } from "react-native";

const info = StyleSheet.create({
  container: {
    justifyContent : 'center',
    alignItems : 'center',
    padding : 10,
  },
  text: {
    fontSize : 17,
    textAlign : 'center',
    fontWeight : 'bold',
    lineHeight : 22,
    color: '#000000',
  },
});

export default info;
